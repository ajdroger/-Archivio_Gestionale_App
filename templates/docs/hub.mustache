<!DOCTYPE html>
<html lang="it" class="h-100">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Knowledge Prime - MCAG Enterprise</title>
    <!-- Core Assets -->
    <link href="{{base_url}}/css/bootstrap.min.css" rel="stylesheet">
    <link href="{{base_url}}/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
    <link href="{{base_url}}/css/components/scroll_navigator.css?v=20260115_REV5" rel="stylesheet">
    
    <style>
        :root {
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.08);
            --primary-glow: 0 0 40px rgba(99, 102, 241, 0.15);
            --accent-gradient: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
        }

        body {
            background-color: #030305;
            background-image: 
                radial-gradient(circle at 15% 50%, rgba(99, 102, 241, 0.08) 0%, transparent 25%),
                radial-gradient(circle at 85% 30%, rgba(168, 85, 247, 0.08) 0%, transparent 25%);
            font-family: 'Outfit', sans-serif;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero-title {
            background: linear-gradient(to right, #ffffff, #94a3b8);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.02em;
        }

        .search-container {
            position: relative;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .search-input {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border-radius: 100px;
            padding: 1.2rem 2rem;
            color: #ffffff !important;
            font-weight: 500;
            font-size: 1.1rem;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }
        
        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.5) !important;
            font-weight: 400;
        }
        
        .search-input:focus {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(99, 102, 241, 0.8);
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.25);
            outline: none;
            color: #ffffff !important;
        }

        /* Glass Cards */
        .doc-card {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            padding: 2rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            height: 100%;
        }

        .doc-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .doc-card:hover {
            transform: translateY(-8px);
            background: rgba(255, 255, 255, 0.06);
            border-color: rgba(255, 255, 255, 0.2);
            box-shadow: var(--primary-glow);
        }

        .doc-card:hover::before {
            opacity: 1;
        }

        .icon-box {
            width: 56px;
            height: 56px;
            border-radius: 16px;
            background: rgba(255, 255, 255, 0.05);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
            transition: transform 0.3s;
        }

        .doc-card:hover .icon-box {
            transform: scale(1.1) rotate(5deg);
            background: var(--accent-gradient);
            color: white !important;
        }

        /* Stats Badge */
        .stat-badge {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            padding: 4px 12px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 0.75rem;
            letter-spacing: 0.05em;
        }

        .action-link {
            text-decoration: none;
        }
        
        .action-link::after {
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            content: '';
            z-index: 10;
        }

        /* Custom Scrollbar for Search Dropdown */
        #docSearchResults::-webkit-scrollbar {
            width: 8px;
        }
        #docSearchResults::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
        }
        #docSearchResults::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 4px;
        }
        #docSearchResults::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 255, 255, 0.4);
        }
    </style>
</head>
<body class="d-flex flex-column h-100">

    <!-- Top Navigation (Project Coherent) -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top py-3" style="background-color: rgba(0,0,0,0.85); backdrop-filter: blur(12px); border-bottom: 1px solid rgba(255,255,255,0.1);">
        <div class="container px-4">
            <a class="navbar-brand d-flex align-items-center" href="{{base_url}}/">
                 <i class="fa-solid fa-shield-halved me-2 text-warning"></i>
                 <span class="fw-bold tracking-wide">MCAG KNOWLEDGE</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarHub">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarHub">
                <ul class="navbar-nav mx-auto">
                    <!-- Optional Central Links could go here -->
                </ul>
                <div class="d-flex align-items-center gap-3">
                     <span class="text-secondary small d-none d-lg-block me-3">Sei in: <strong class="text-white">Documentation Hub</strong></span>
                     <a href="{{base_url}}/" class="btn btn-sm btn-outline-secondary rounded-pill px-3"><i class="fa-solid fa-right-from-bracket me-2"></i>Torna alla Dashboard</a>
                     
                     <!-- User Dropdown (Operational) -->
                     <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle p-1 rounded-pill border border-secondary bg-dark bg-opacity-50" id="hubUserDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold shadow-sm" style="width: 34px; height: 34px;">{{user_initial}}</div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-lg border-secondary bg-dark text-white" aria-labelledby="hubUserDropdown" style="background: rgba(10, 10, 15, 0.95) !important; backdrop-filter: blur(10px);">
                            <li><h6 class="dropdown-header text-uppercase x-small text-warning fw-bold"><i class="fa-solid fa-shuttle-space me-2"></i>Mission Control</h6></li>
                            
                            <li><a class="dropdown-item text-white" href="{{base_url}}/impostazioni"><i class="fa-solid fa-sliders me-2"></i>Settings Logic</a></li>
                            <li><a class="dropdown-item text-warning" href="{{base_url}}/devtools"><i class="fa-solid fa-microchip me-2"></i>DevTools Hub</a></li>
                            
                            <!-- Specific DevTools Modules -->
                            <li><a class="dropdown-item text-light small ps-4" href="{{base_url}}/devtools#terminal"><i class="fa-solid fa-terminal me-2 text-danger"></i>Terminal</a></li>
                            <li><a class="dropdown-item text-light small ps-4" href="{{base_url}}/devtools#db"><i class="fa-solid fa-database me-2 text-info"></i>Database</a></li>
                            <li><a class="dropdown-item text-light small ps-4" href="{{base_url}}/devtools#security"><i class="fa-solid fa-shield-cat me-2 text-success"></i>Security</a></li>
                            <li><a class="dropdown-item text-light small ps-4" href="{{base_url}}/devtools#editor"><i class="fa-solid fa-folder-tree me-2 text-warning"></i>File System</a></li>
                             <li><a class="dropdown-item text-light small ps-4" href="{{base_url}}/devtools#audit"><i class="fa-solid fa-file-waveform me-2 text-secondary"></i>Log Viewer</a></li>

                            <li><hr class="dropdown-divider border-secondary"></li>
                            
                            <li><a class="dropdown-item text-info bg-dark-hover" href="{{base_url}}/docs/hub"><i class="fa-solid fa-folder-open me-2"></i>Documentation Hub</a></li>
                            <li><hr class="dropdown-divider border-secondary"></li>
                            
                            <li><a class="dropdown-item text-danger" href="{{base_url}}/logout"><i class="fa-solid fa-power-off me-2"></i>Esci dal sistema</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow-1 pt-5 mt-5">
        <!-- Hero Header -->
        <div class="container py-5 text-center">
            <span class="badge rounded-pill border border-info border-opacity-25 bg-info bg-opacity-10 text-info px-3 py-2 mb-4 fw-light tracking-widest">
                <i class="fa-solid fa-sparkles me-2"></i>KNOWLEDGE ENGINE V9.1
            </span>
            <h1 class="display-3 fw-bold hero-title mb-4">Documentation Prime</h1>
            <p class="lead text-secondary mb-5 mx-auto" style="max-width: 600px;">
                Accesso unificato all'intelligenza del progetto. Esplora l'analisi strategica, l'architettura tecnica e le guide operative.
            </p>
            
            <div class="search-container mb-5 position-relative">
                <i class="fa-solid fa-search position-absolute text-secondary" style="left: 20px; top: 50%; transform: translateY(-50%); z-index: 5;"></i>
                <input type="text" id="docSearchBox" class="form-control search-input ps-5" placeholder="Cerca classi, analitiche o normative (es. 'GDPR')..." autocomplete="off">
                
                <!-- Search Results Dropdown -->
                <div id="docSearchResults" class="position-absolute w-100 mt-3 rounded-4 shadow-lg d-none" style="background: rgba(20, 20, 25, 0.98); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.1); z-index: 1000; max-height: 400px; overflow-y: auto;">
                    <!-- Results injected here via JS -->
                </div>
            </div>
        </div>

        <!-- Content Grid -->
        <div class="container pb-5">
            <div class="row g-4">
                {{#categories}}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3">
                    <div class="doc-card">
                        <div class="d-flex justify-content-between align-items-start">
                            <div class="icon-box text-white-50">
                                <i class="fa-solid {{icon}}"></i>
                            </div>
                            <span class="stat-badge text-secondary">
                                {{count}} FILES
                            </span>
                        </div>
                        
                        <h4 class="text-white fw-medium mb-2">{{name}}</h4>
                        <p class="text-secondary small mb-4 opacity-75" style="min-height: 40px; line-height: 1.6;">
                            {{description}}
                        </p>
                        
                        <div class="d-flex align-items-center text-info small fw-bold">
                            ACCESS <i class="fa-solid fa-arrow-right-long ms-2"></i>
                        </div>
                        
                        <a href="{{path}}" class="action-link"></a>
                    </div>
                </div>
                {{/categories}}
            </div>
        </div>
    </main>

    <footer class="footer mt-auto bg-dark border-top border-secondary" style="background: rgba(3, 3, 5, 0.95) !important;">
        <!-- Main Footer Content -->
        <div class="container-fluid px-5 py-5">
            <div class="row g-4">
                <!-- Column 1: Brand & Mission -->
                <div class="col-lg-4 col-md-6 mb-4 mb-lg-0">
                    <h5 class="text-white fw-bold mb-3">
                        <i class="fa-solid fa-shield-halved text-warning me-2"></i>MCAG ARCHIVIO
                    </h5>
                    <p class="text-secondary small mb-3" style="max-width: 300px;">
                        Piattaforma gestionale Mission-Critical per l'amministrazione, l'audit e la sicurezza dei dati anagrafici.
                        Standard militari di resilienza e integrità.
                    </p>
                    <div class="d-flex gap-2">
                        <span class="badge bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25">
                            <i class="fa-solid fa-layer-group me-1"></i>v7.4.0
                        </span>
                        <span class="badge bg-success bg-opacity-10 text-success border border-success border-opacity-25">
                            <i class="fa-solid fa-server me-1"></i>Stable
                        </span>
                    </div>
                </div>

                <!-- Column 2: Navigazione Rapida -->
                <div class="col-lg-2 col-md-6 mb-4 mb-lg-0">
                    <h6 class="text-white text-uppercase fw-bold small mb-3">Navigazione</h6>
                    <ul class="list-unstyled text-secondary small">
                        <li class="mb-2"><a href="{{base_url}}/" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-gauge-high me-2 opacity-50"></i>Dashboard</a></li>
                        <li class="mb-2"><a href="{{base_url}}/soci" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-users me-2 opacity-50"></i>Registro Soci</a></li>
                        <li class="mb-2"><a href="{{base_url}}/statistiche" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-chart-line me-2 opacity-50"></i>Report Center</a></li>
                        <li class="mb-2"><a href="{{base_url}}/devtools" class="text-decoration-none text-warning hover-white"><i class="fa-solid fa-microchip me-2 opacity-50"></i>Mission Control</a></li>
                    </ul>
                </div>

                <!-- Column 3: Legal & Compliance -->
                <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                    <h6 class="text-white text-uppercase fw-bold small mb-3">Legal & Compliance</h6>
                    <ul class="list-unstyled text-secondary small">
                        <li class="mb-2"><a href="{{base_url}}/landing/legal/privacy.html" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-user-shield me-2 opacity-50"></i>Privacy Policy (GDPR)</a></li>
                        <li class="mb-2"><a href="{{base_url}}/landing/legal/cookie_policy.html" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-cookie-bite me-2 opacity-50"></i>Cookie Policy</a></li>
                        <li class="mb-2"><a href="{{base_url}}/landing/legal/EULA.html" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-file-contract me-2 opacity-50"></i>EULA (Termini)</a></li>
                        <li class="mb-2"><a href="{{base_url}}/landing/legal/SLA_MAINTENANCE.html" class="text-decoration-none text-secondary hover-white"><i class="fa-solid fa-headset me-2 opacity-50"></i>SLA & Supporto</a></li>
                        <li class="mb-2"><span class="text-success"><i class="fa-solid fa-check-circle me-2"></i>Audit Compliant</span></li>
                    </ul>
                </div>

                <!-- Column 4: Sviluppo & Contatti -->
                <div class="col-lg-3 col-md-6">
                    <h6 class="text-white text-uppercase fw-bold small mb-3">Sviluppo & Supporto</h6>
                    <ul class="list-unstyled text-secondary small">
                        <li class="mb-2"><i class="fa-solid fa-code me-2 opacity-50"></i>Dev: <strong>Soobadur Mohammad Ajmeer</strong></li>
                        <li class="mb-2"><i class="fa-solid fa-building me-2 opacity-50"></i>P.IVA: 12345678901</li>
                        <li class="mb-2"><i class="fa-solid fa-envelope me-2 opacity-50"></i>support@mcag-system.interno</li>
                        
                        <li class="mt-3">
                            <small class="d-block text-muted fst-italic">
                                "Secure Digitalization Project"<br>
                                Building the future of archives.
                            </small>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Copyright Bar -->
        <div class="bg-black bg-opacity-50 py-3 border-top border-secondary">
            <div class="container-fluid px-5">
                <div class="row align-items-center">
                    <div class="col-md-6 text-center text-md-start">
                        <p class="mb-0 text-secondary x-small">
                            &copy; 2026 <strong>MCAG System</strong>. Tutti i diritti riservati.
                        </p>
                    </div>
                    <div class="col-md-6 text-center text-md-end mt-2 mt-md-0">
                         <span class="text-secondary x-small font-monospace opacity-75">
                            APP_VER: <span class="text-warning">v7.4.0</span> | Operational Command
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="{{base_url}}/js/lib/bootstrap.bundle.min.js"></script>

    <!-- Scroll Navigator -->
    <script src="{{base_url}}/js/components/scroll_navigator.js"></script>

    <script>
        // Documentation Search Logic (Vanilla JS)
        document.addEventListener('DOMContentLoaded', () => {
            const searchInput = document.getElementById('docSearchBox');
            const resultsContainer = document.getElementById('docSearchResults');
            let debounceTimer;

            searchInput.addEventListener('input', (e) => {
                const query = e.target.value.trim();
                
                clearTimeout(debounceTimer);
                
                if (query.length < 3) {
                    resultsContainer.classList.add('d-none');
                    resultsContainer.innerHTML = '';
                    return;
                }

                debounceTimer = setTimeout(() => {
                    fetchResults(query);
                }, 300); // 300ms debounce
            });

            async function fetchResults(query) {
                try {
                    resultsContainer.innerHTML = '<div class="p-3 text-center text-muted"><i class="fa-solid fa-circle-notch fa-spin me-2"></i>Searching knowledge base...</div>';
                    resultsContainer.classList.remove('d-none');

                    const response = await fetch(`{{base_url}}/docs/search?q=${encodeURIComponent(query)}`);
                    const data = await response.json();

                    if (data.length === 0) {
                        resultsContainer.innerHTML = '<div class="p-3 text-center text-muted">Nessun risultato trovato.</div>';
                    } else {
                        renderResults(data);
                    }
                } catch (error) {
                    console.error('Search error:', error);
                    resultsContainer.innerHTML = '<div class="p-3 text-center text-danger">Errore durante la ricerca.</div>';
                }
            }

            function renderResults(results) {
                let html = '<div class="list-group list-group-flush">';
                results.forEach(item => {
                    html += `
                        <a href="${item.url}" class="list-group-item list-group-item-action bg-transparent border-bottom border-secondary border-opacity-25 p-3 text-white">
                            <div class="d-flex w-100 justify-content-between align-items-center mb-1">
                                <h6 class="mb-0 fw-bold text-info"><i class="fa-regular fa-file-lines me-2"></i>${item.title}</h6>
                                <small class="text-secondary text-uppercase x-small border border-secondary px-2 rounded-pill">${item.category}</small>
                            </div>
                            <p class="mb-1 small text-white-50 fst-italic">${item.snippet}</p>
                        </a>
                    `;
                });
                html += '</div>';
                resultsContainer.innerHTML = html;
            }

            // Close search when clicking outside
            document.addEventListener('click', (e) => {
                if (!searchInput.contains(e.target) && !resultsContainer.contains(e.target)) {
                    resultsContainer.classList.add('d-none');
                }
            });
        });
    </script>
</body>
</html>
